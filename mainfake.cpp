
#define SDL_MAIN_HANDLED
#include "SDL/SDL.h"

int main(int argc, char *argv[]) {

  // glfwInit();
  // glfwWindowHint(GLFW_CLIENT_API, GLFW_NO_API);

  // Windows resize
  // glfwWindowHint(GLFW_RESIZABLE, GLFW_TRUE);

  // glfwCreateWindow(20, 20, "Hello Vulkan", nullptr, nullptr);
  return 0;
}
